services:
  olivetin:
    image: jamesread/olivetin:latest
    container_name: portal-console-olivetin
    restart: unless-stopped
    volumes:
      - ./olivetin:/config
    ports:
      - "3002:1337"
