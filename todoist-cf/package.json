{
  "name": "todoist-cf",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute todoist_cf --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^3.114.0"
  }
}
